<!DOCTYPE HTML>
<html>
    <head>
        <title>Most Recent Trip</title>
    </head>
    <body>
        {{ range $i, $e := $ }}
            <h1>{{$e.Person.Name}}</h1>
            <p>{{$e.Entry.Entry}}</p>
            <p>{{$e.Entry.String}}</p>
        {{ end }}
    </body>
</html>
